/**
 * Acts as global obj for passing data
 *
 * Variable List
 * - DOM: main, ui, can, ctx
 * - Game: game_size
 */
class AceVox {
  constructor() {
    //Do nothing here
  }
}

export default (new AceVox);
