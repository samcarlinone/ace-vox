/**
 * Acts as global obj for passing data
 *
 * Variable List
 * - DOM: main, ui, can, gl
 * - Game: game_size
 */
class AceVox {
  constructor() {
    //Do nothing here
  }
}

export default (new AceVox);
